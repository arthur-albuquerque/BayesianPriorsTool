% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mu_prior_plot.R
\name{mu_prior_plot}
\alias{mu_prior_plot}
\title{Plot a Normal Distribution Prior with Quantile Interval}
\usage{
mu_prior_plot(
  extreme_value,
  scale = c("absolute", "relative"),
  mean = NULL,
  prob = 0.95,
  xlabel,
  inner_color = "orange",
  tail_color = "gray90"
)
}
\arguments{
\item{extreme_value}{Numeric value representing the upper limit of the interval (must be positive if `scale = "relative"`).}

\item{scale}{Character string specifying the scale: `"absolute"` (default) or `"relative"`.}

\item{mean}{Numeric value for the mean of the distribution. Defaults to `0` if `scale = "absolute"`, and `1` if `scale = "relative"`. Must be positive if `scale = "relative"`.}

\item{prob}{Numeric value between 0 and 1 specifying the quantile interval probability (default is 0.95).}

\item{xlabel}{Character string for the x-axis label; must be provided by the user (e.g., "Odds Ratio").}

\item{inner_color}{Character string specifying the fill color for the inner quantile interval (default is "orange").}

\item{tail_color}{Character string specifying the fill color for the tails outside the quantile interval (default is "gray90").}
}
\value{
A `ggplot2` object representing the normal distribution plot.
}
\description{
Creates a density plot of a normal distribution based on an upper limit, with a highlighted
quantile interval (default 95%). Designed for visualizing priors of the overall effect
parameter (mu) in Bayesian random-effects meta-analysis models.
}
\details{
This function accounts for the scale of the distribution. For example, effect sizes such as
odds ratios, risk ratios, and hazard ratios are analyzed on the log scale (termed "relative" scale).
In contrast, effect sizes like risk differences, mean differences, and standardized mean
differences are on the "absolute" scale.
}
\examples{
# Absolute scale plot
mu_prior_plot(extreme_value = 5, scale = "absolute", xlabel = "Mean Difference")
# Relative scale plot
mu_prior_plot(extreme_value = 2, scale = "relative", xlabel = "Odds Ratio")
# Custom mean and prob
mu_prior_plot(extreme_value = 10, scale = "absolute", mean = 2, prob = 0.9,
              xlabel = "Effect Size")
}
